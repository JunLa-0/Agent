# 项目理解

用户输入->意图识别Agent->协调调度Agent->6个Agent并行/串行->聚合结果+记忆更新

意图识别IntentionAgent的关键设计是两段式输出reasoning+decision，可以提升意图识别的准确率，就是从65%提升到90%，而且不是关键词匹配，是纯LLM进行语义匹配，输出结构化JSON：推理过程+多意图识别+Query改写+Agent调度计划

OrchestrationAgent协调调度，按照优先级来进行分组，如果优先级相同就按照asyncio.gather并行执行，而且还进行了异常隔离，单个Agent执行失败不会对其他Agent带来影响

LazyAgentRegistry实现懒加载、插件化架构、动态发现：插件化架构所有的子Agent在.claude/skills/目录下，每个Skill都有独立的SKILL.md+script/agent.py，动态发现：启动时扫描目录，注册Skill元数据，懒加载__getitem__时才真正importlib加载模块+实例化Agent，Progressive Disclosure意图识别阶段只加载元数据，就是只加载SKILL.md的description，执行阶段才加载完整代码



# 项目亮点

## 亮点1：多Agent协作与优先级并行调度（Agent开发岗核心亮点）

> 设计了Plan-and-Execute架构的多Agent协作系统，包含1个意图识别Agent + 1个协调器Agent + 6个子Agent。意图识别采用LLM语义理解（非关键词匹配），输出结构化推理过程+调度决策；协调器基于优先级分组，同优先级Agent通过`asyncio.gather`并行执行，不同优先级串行依赖，系统响应时间从30秒优化到15秒（-50%）。

**Plan and Execute相关考察，例如为什么不用ReAct模式**

**并行执行的异常隔离怎么做，multi-agent如何处理**

**意图识别的正确率怎么保证**

----

## 亮点2：插件化Agent架构 + 懒加载 + Progressive Disclosure（Agent开发岗+后端岗）

> 设计了Skill Plugins插件化架构，所有子Agent重构为独立插件（`.claude/skills/`目录），通过`LazyAgentRegistry`实现动态发现与懒加载——启动时仅扫描元数据（SKILL.md），首次调用时才通过`importlib`动态加载代码并实例化。引入Progressive Disclosure机制，意图识别阶段只暴露Skill的description元数据，执行阶段才注入完整Prompt指令，启动速度优化到3秒。

**插件化有什么好处**

**懒加载如何实现**

**Progressive Disclosure是什么**

---

## 亮点3：RAG知识库 + 本地化Embedding（大模型应用岗核心亮点）

> 构建了基于Milvus向量数据库的RAG知识库系统，集成BGE-small-zh-v1.5中文Embedding模型（本地部署，不依赖HuggingFace），实现文档智能分块+余弦相似度检索+LLM生成式问答。在Prompt层强制约束"知识库无相关信息时回答不知道"，防止大模型幻觉，知识库问答准确率达95%。支持8类企业差旅文档的知识溯源。

**RAG检索相关知识，RAG的chunking策略**

**向量数据库如何选型**

**如何防止幻觉**

-----

## 亮点4：两层记忆系统 + 智能偏好管理（大模型应用岗+后端岗）

> 设计了短期记忆（滑动窗口10轮对话）+ 长期记忆（持久化存储+LLM异步总结）的两层记忆架构。长期记忆支持用户偏好的智能管理：通过LLM语义理解识别"我还喜欢如家"（追加append）vs "我搬家到上海了"（覆盖replace），实现偏好类型的动态扩展。集成LLM异步总结能力，自动对历史聊天记录和行程记录做摘要，作为上下文注入意图识别阶段。

**短期记忆为什么用滑动窗口**

**长期记忆的偏好管理怎么实现append/replace**

**LLM异步总结的作用**

----

## 亮点5：熔断器 + 指数退避重试 + 健康检查（后端开发岗亮点）

> 实现了生产级的LLM调用稳定性保障：熔断器（三态CLOSED/OPEN/HALF_OPEN，连续失败5次自动熔断，60秒后半开试探恢复）+ 指数退避重试（仅对超时/429/5xx可重试错误生效，最大3次，带随机抖动防雪崩）+ 健康检查（`python cli.py health`独立探测，退出码0/1便于监控集成）。

**熔断器的三态模型**

**为什么重试要区分错误类型**

**指数退避为什么加抖动**

----

