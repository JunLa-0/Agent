# 实习介绍和业务描述

在实习过程中我主要负责后端工作的工作，负责支撑几十万用户的众包互助平台。在实习期间完成10+接口的重构，通过索引优化和缓存分流将系统整体查询性能提升了60%，并且辅助完成资金结算链路的一致性治理方案。

我负责的这个项目是一个C2C的项目，用户与用户直接进行对接、直接进行交易的一个众包互助平台，我们公司主要就是提供平台和技术支持，主要提取为三个核心的业务逻辑：

- 发单流程：悬赏主在app中发布订单，并且在平台上预支赏金
- 接单流程：做单人根据自己需求和悬赏主的要求选择合适的订单，在约定的时间内完成任务提交材料证明
- 平台结算：悬赏主审核材料通过后平台就会把预先支付的赏金发送给做单人
- 对于这三个核心的业务逻辑的实现，主要在TaskOrderServiceImpl这个实现类中处理任务状态机的交换，保证资金结算链路的原子下，确保发单人和做单人的利益平衡

主要使用到的技术栈：SpringBoot、MyBatis、MySQL、Redis、RabbitMQ

在工作过程中，主要遇到的一些问题，例如：

- 高并发情况下如何保证数据一致性：做单人不能重复领取同一笔任务，领取同一个任务的人数不能超过任务人数限制，因为每一个任务都是有人数限制的
- 保证资金结算和发放不出错：因为每天都会产生海量的小额交易订单，必须保证平台账目不能出错
- 风控反作弊系统：防止刷单，在短时间内直接把任务人数直接刷完，防止虚假证据审核
- 高性能统计：因为有一个实时排行榜，在海量订单数据中快速生成排行榜
- 系统性能优化：随着用户量增加，会带来响应延迟的问题，所以进行数据库和缓存的联合调优，通过对RankingServiceImpl这个实现类的缓存架构进行重构，使用多级缓存进行重构，并且使用核心表，如订单、流水表的SQL索引优化，显著提升了P99的响应时间，提升了复杂筛选场景下的查询吞吐量
- 资金安全：使用Redis+AOP实现了全局幂等组件，重构WithdrawController中的体现审计流，解决了弱网环境重试下的重复打款风险，而且还引入了异常订单的自动对账补偿机制

----

## 实时排行榜从循环查库到批量映射+缓存预热

*   **源码痛点**：在 [RankingServiceImpl.java](file:///Users/wenjunlang/Downloads/qie_back/src/main/java/com/qiehuzhu/app/service/impl/RankingServiceImpl.java) 中，获取排行榜需要从 Redis 拿到用户 ID 列表后，再循环调用 `userService.findById` 获取昵称和头像，产生了严重的 **N+1 查询问题**。
*   **优化产出**：
    *   **方案**：重构为 **SQL 批量查询（`IN` 语句）**，并对 Top 50 的用户信息引入 **本地二级缓存（Caffeine）**。
    *   **性能提升**：接口响应时间（50 人榜单）从 **480ms 降低至 25ms**。
    *   **大厂亮点**：解决了分布式环境下高频小查询对数据库连接池的挤占。

## 缓存策略深度优化：排行榜响应速度与一致性保障

重构RankingServiceImpl类中的缓存逻辑，引入了多级缓存和异步刷新策略

核心排行榜接口的响应时间从450ms优化到稳定在60ms以内

引入布隆过滤器防止非法ID查询，将无效的DB回源请求降低到90%

----

## 基于SQL优化和报表开发

针对原有平台经营统计Job执行缓慢的问题，通过对task_order和fund_detail等大表添加缺失的索引，将报表生成的执行耗时从30s缩短至3s

---

## 异步通信与第三方接口对接

主要是负责对接第三方支付回调，例如支付宝、微信等，以及系统内部的消息推送逻辑

使用RabbitMQ实现了注册奖励发放的异步化，将注册主接口的响应耗时从800ms降低至150ms，通过引入重试机制，解决了网络抖动的情况下第三方回调丢失导致的状态不更新问题

---

## 资金结算体系的自动化风控与降本增效

在WithdrawController这个类中的提现链路中设计并落地了一套基于异常规则的自动化审计流

通过对IP变动、接单耗时异常、账号关联性等维度的自动化校验、使80%的小额体现实现秒级自动审批，在实习期间识别并拦截了300多个疑似羊毛党黑产账号，为平台规避了月2.4万元每月的恶意体现带来的损失

----

## 大数据量下的分页性能优化

*   **源码痛点**：`fund_detail` 表是项目中最肥的表。原有分页查询使用 `LIMIT offset, size`，当用户查询较早前的流水时（深分页），性能出现指数级下降。
*   **优化产出**：
    *   **方案**：采用 **“延迟关联”** 优化策略。先通过子查询查出主键 ID，再关联获取详情，避免了大量无效行扫描。
    *   **性能提升**：深分页（第 1000 页以后）耗时从 **3.5s 降低至 180ms**。
    *   **大厂亮点**：解决了海量数据场景下的典型性能瓶颈，具备处理亿级数据的思维。

---

# 面试问题

## 放重复提交的AOP切面是如何实现的，Redis Key是怎么设计的

基于Spring AOP + 自定义注解@NoRepeatSubmit，在@Before通知里用Redis的SETNX实现分布式锁，Key的设计是类名方法名token，保证同一用户对同一接口在规定时间内不能重复请求

```java
// key的设计是：类名_方法名_token
// 通过自定义符合业务需求的key，自定义一个命名方法
String redisKey = target + "_" + methodName + "_" + token;
Boolean setIfAbsent = redisTemplate.opsForValue().setIfAbsent(redisKey, "操作频繁", noRepeatSubmit.time(), TimeUnit.MILLISECONDS);
```

----

## 这个key的设计有什么问题，如果用户在短时间内提交两笔金额不同的提现，会被拦截吗

会，这个key设计是有缺陷的，当前key只到达了方法级别，没有拼接业务唯一标识，例如提现金额、订单号、请求体摘要，合理的做法是：对请求体进行MD5加密，相同的请求就拦截，不同请求就放行

```java
String bodyDigest = DigestUtils.md5Hex(JSON.toJSONString(args[0]));
String RedisKey = target + "_" + methodName + "_" + token + "_" + bodyDigest;
```

或者更进一步的改进：使用业务流水号作为key，通过前段生成clientRequestId传给后端，这就实现了真正的幂等保证

-----

## SETNX有什么缺陷，如果业务执行成功了但锁过期时间到了，或者锁还没过期但业务已经失败了，怎么办

SETNX设置的过期时间是一个固定值，假设设计的过期时间是3s，当业务执行时间超过了3s，锁自动释放，后续的重复请求就能进入，SETNX就起不到作用了

解决方案：

- 可以使用Redisson的看门狗机制来自动续期
- 在finally块主动释放锁
- 合理评估业务的执行时间，评估超时时间

业务失败了，但是锁还没到期：当前代码直接抛出异常，但是锁还没有到期，所以锁没有释放，用户要等锁过期了，即锁释放了才能重试，应该在catch中主动删除key

```java
try{
  //业务逻辑
} catch (Exception e) {
  redisTemplate.delete(redisKey);
  throw e;
}
```

---

## 在分布式环境下，如果Redis主节点挂了，从节点还没同步这个key，会发生什么，怎么解决

就是Redis主从切换导致锁丢失的问题，SETNX写入主节点，主节点宕机后key还没来得及同步到从节点，新主节点没有这个key，另一个请求就能拿到锁

解决方案：

- RedLock算法，Redisson实现：向N个独立的Redis节点，通常是5个，依次申请锁，超过半数成功才能获取成功，缺点就是性能开销大，存在时钟漂移争议
- ZooKeeper临时顺序节点：利用ZK的CP特性，节点挂掉后临时节点自动删除，强一致性保证，缺点是吞吐量不如Redis
- MySQL乐观锁：对于提现这种强一致性的场景而言，直接在数据库层面要弄哦个版本号或状态机保证

---

## 排行榜如何实现，为什么用ZSet实现排行榜

用Redis的Sorted Set 就是ZSet来实现排行榜，member是用户ID的字符串，score是排名分数，收入进入/完成任务数=排名分数，ZSet底层是跳表+哈希表实现

````java
Set<ZSetOperations.TypedTuple<String> rangeWithScores = redisTemplateopsForZSet().reverseRangeWIthScores(rank, start, end);
````

---

## 排行榜取出来后，用户信息是怎么查的，这里有什么性能问题

当前实现是从ZSet拿到用户id，即userId列表后，用SELECT WHERE id IN（?）批量查用户表，这个做法是正确的，但是有个问题：IN查询不保证返回顺序和IN列表是一致的，代码里直接用查出来的顺序映射score列表，在MySQL中WHERE id IN(1,3,2)返回的顺序可能是1，2，3.欧意应该在Java层用Map去做匹配，或者用MySQL的FIELD()函数保证顺序

如果排行榜数据量很大，千万级别的用户，就得去考虑：

- 分段存储：按分数段拆成多个ZSET，减少单key大小
- 定时归档：历史榜单落库，Redis只保留当天热数据
- 估算排名：非精确场景用HyperLogLog或分桶估算

---

## 如果排行榜要做到实时更新+千万级用户+秒级查询，架构怎么设计

- 写入：用Kafka消费用户行为时间，异步更新ZSET score(ZINCRBY)，削峰填谷
- 读取：Redis Cluster分片，按榜单类型+ 日期做key路由
- 容灭：Redis持久化，RDB和AOF混合持久化，同时异步写一份到TiDB/ClickHouse做数据回溯
- 热点处理：对于Top100这种高频查询，加本地Caffeine缓存，1秒TTL，允许短暂的数据不一致

----

## 提现接口做了那些校验，如果让你设计一个完整的防刷机制，你会加什么

当前校验包括：黑名单检查、金额上下限、提现次数限制、实名认证、支付宝帐户同名限制、余额检查、手续费计算

明显的安全漏洞：

- 没有进行IP风控：同一IP在短时间大量提现请求没有拦截，要检测IP变动去进行相应的拦截
- 没有设备指纹：多帐号用同一台设备提现无法识别
- 没有提现时间异常检测：凌晨三点大量提现没有告警
- 支付宝帐户关联检测太弱：只是简单统计同名次数大于等于10，没有做或者那个胡关联图谱

**扩展方案**：



```
风控引擎分层设计：
L1（硬规则）：黑名单、金额阈值、频率限制 → 直接拒绝
L2（软规则）：IP聚集度、设备关联、行为序列异常 → 打分，超过阈值转人工
L3（离线分析）：账户关联图谱、资金链路追踪 → T+1 产出风险名单
```

---

## 提现申请和余额扣减怎么保证一致性，如果先扣余额再插提现记录，扣完余额后服务挂了怎么办

**回答**：当前代码只插入了提现申请记录（status=APPLY），真正的余额扣减不在这个事务里。这是合理的——提现是异步审核流程，申请时不扣余额，审核通过后才扣。

如果涉及到实时扣减的场景，标准做法是：

1. **数据库事务**：扣余额 + 插流水在同一个 `@Transactional` 里
2. **乐观锁**：`UPDATE fund SET balance = balance - ? WHERE user_id = ? AND balance >= ?`
3. **TCC 分布式事务**：Try（冻结余额）→ Confirm（扣减+打款）→ Cancel（解冻），适用于跨系统转账
4. **Outbox 模式**：本地事务写业务数据+outbox 消息，然后 CDC 投递到 MQ 保证最终一致性

----

## 四、任务接取 & 审核流程

### Q10: 用户接任务的时候做了哪些校验？这些 SQL 有什么问题？

**回答**：[TaskOrderServiceImpl.java:176-269](vscode-webview://0menaaat83vhfobb3s35potm91iefcs9ijd3f7g1vrh5bq5tgu00/src/main/java/com/qiehuzhu/app/service/impl/TaskOrderServiceImpl.java#L176-L269) 里的 `acceptTask` 方法依次做了：同名任务检查、举报检查、占用数检查、等级限制检查、未绑定微信限制检查。每次检查都是一条独立的 SQL。

**问题**：N+1 查询，每个校验单独查库。高并发下数据库压力大。

**优化方案**：

- 把多个校验合并成一个 SQL（JOIN 子查询或 UNION ALL 一次查出）
- 对于高频校验（如等级限制），用 Redis 缓存用户等级信息
- 对于黑名单检查，用 Bloom Filter 前置过滤，减少无效查询

### Q11（场景扩展）: 如何防止用户用脚本批量接任务薅羊毛？

**回答**：

- **频率限制**：Redis 滑动窗口限流，同用户 1 分钟内最多接 N 个任务
- **行为分析**：接任务时间间隔、完成任务耗时分布，异常模式自动触发验证码
- **设备绑定**：一个设备只能操作有限个账号
- **收益上限**：单用户每日收益封顶，超过后需人工审核
- **图灵验证**：接任务达到阈值后弹出滑块验证码

------

## 五、代码质量问题（面试官可能主动发现）

### Q12: 我看到你的 NoRepeatSubmitAspect 和 NoRepeatSubmitAspect2 几乎一模一样，为什么会有两个？

**回答**：这是历史遗留问题。NoRepeatSubmitAspect2 是为了给 `/api/v1/taskOrder/findAll` 这个查询接口单独做的防重复提交，区别只是 key 里多拼了一个 `query.getStatus()`。正确的做法是用同一个切面，通过 SpEL 表达式动态解析 key：



```java
// 改进：用一个注解搞定
@NoRepeatSubmit(key = "taskOrder_findAll_#query.status", time = 100)
```

这体现了**代码复用**和**开闭原则**的重要性。

### Q13: 外部渠道判断那段代码在多个方法里重复出现，你觉得有什么问题？

**回答**：这段代码在 `acceptTask`、`withdrawApplyReal` 等多个方法里完全重复（[TaskOrderServiceImpl.java:179-198](vscode-webview://0menaaat83vhfobb3s35potm91iefcs9ijd3f7g1vrh5bq5tgu00/src/main/java/com/qiehuzhu/app/service/impl/TaskOrderServiceImpl.java#L179-L198) vs [WithdrawServiceImpl.java:1440-1459](vscode-webview://0menaaat83vhfobb3s35potm91iefcs9ijd3f7g1vrh5bq5tgu00/src/main/java/com/qiehuzhu/app/service/impl/WithdrawServiceImpl.java#L1440-L1459)）。应该抽成一个公共方法或拦截器：



```java
// 方案1：抽成工具方法
public ExternalChannel resolveExternalChannel(HttpServletRequest request) { ... }

// 方案2：做成 HandlerInterceptor，在 Controller 层统一处理，把结果放进 request attribute
```

------

## 六、系统设计场景题（大厂最爱）

### Q14: 如果这个任务平台的 QPS 从 100 涨到 10000，你会从哪些方面优化？

**回答**：

1. **读多写少场景**：Redis 缓存热点数据（任务列表、排行榜），设置合适的 TTL，用 Cache Aside 模式
2. **写场景（接任务、提现）**：MQ 异步削峰，核心操作（余额扣减）用 Redis Lua 脚本原子化
3. **数据库**：读写分离，按 user_id 分库分表，冷热数据分离
4. **接口限流**：网关层（Nginx/Sentinel）做全局限流，内部做用户级令牌桶限流
5. **监控告警**：Prometheus + Grafana 监控慢查询、错误率、RT 分位数

### Q15: 排行榜从日榜改成实时榜，ZSET 的 score 更新频率从每天一次变成每秒数千次，Redis 会有什么问题？怎么解决？

**回答**：

- **热 key 问题**：热门用户的 ZSET 操作集中在一个分片，CPU 打满。解决方案：① key 拆分（按用户 ID 哈希打散到多个 ZSET，查询时合并）；② 本地缓存聚合，1 秒批量写入一次
- **大 key 问题**：ZSET 成员过多（千万级），`ZREVRANGE` 变慢。解决方案：① 只保留前 N 名在 Redis，其余落 MySQL；② 分层存储（L1: Top1000 Redis，L2: ClickHouse 全量）
- **内存**：ZSET 每个 member 约 72 字节 overhead，1 亿 member ≈ 7GB。评估成本，考虑压缩或采样

-----

# 针对第一条实习产出

以发送随即通知的业务为例子，他是需要先加载10条任务完成记录，然后再发送通知，那么在进行重构优化前，是通过一条SQL直接查询出10条已完成的任务记录，然后我需要知道每一条记录的用户信息和任务信息，其实就是用户id和任务id，所以就要根据TaskOrder去查询UserId和TaskId，那就进行for循环，循环10次，一次只能查询出一个用户ID和任务ID，他的调用链路就是在Controller层的randomNotice这个类发送Get请求给Service层去实现具体业务，在Service去进行TaskOrder已经完成的任务的查询，查询10条，正常情况下需要花费15ms的时间，然后查询完已完成任务的记录后就去查询每一条任务的任务id和用户id，就要通过Select语句来进行查询，select * from user where ID= 和select * from task where ID=，每一条sql都需要花费3ms的时间，那循环100次就得消耗60ms，那么在重构之前最佳情况是消耗约75ms的时间

进行优化重构后，即通过SQL批量查询和caffeine本地缓存的优化，主要就是在SQL查询和后续的查数据库的过程中差异，Controller层中的操作还是一样的，发送请求过来，用来limit查询，在查询10条已完成的任务记录的时候就会同步查询出userid，那么还是消耗15ms的一个时间，然后把查询出来的usrid放入到一个list列表中，taskid也一样，原本是需要进行20条sql的查询，现在只需要进行1条sql查询即可实现，要消耗8ms，然后把查询出来的结果存入caffeine数据库，在查询的时候还会去判断缓存是否命中，如果缓存命中就跳过数据库查询直接返回，如果缓存没有命中就进行批量查询，把查询的结果填入缓存中，如果缓存命中耗时就仅仅15ms。如果是第一次查询，缓存无法命中，就是23ms，从20条SQL查询用户id和任务id优化成2条sql查询

所以缓存不命中就3条sql，消耗25ms，如果命中缓存就1sql，不需要进行批量查询，只消耗15ms

N+1查询问题是指先查询出N条记录，然后再对每条记录再单独查一次关联数据，比如上面说到的随机通知接口，先查10条最新的已完成的任务订单，然后循环10次，去查询对应的用户id和订单id，那么在重构前就一共进行了20+1次SQL

那么我对代码进行重构了，主要修改了就是随机通知接口，从21SQL变成了3条SQL，然后就是TaskOrder，任务订单查询列表，之前是每条订单单出查询审核记录，然后现在修改为批量查询，排行榜在重构前是逐个去查询的，重构之后通过IN批量查询，一般是查询top50的用户信息

那为什么只改这三个接口呢，因为只有这三个接口对N+1影响大，其他受到的影响不大，例如各种用户规则校验，黑名单校验、管理员操作这些接口调用量比较低，OPS基本上都是个位数两位数这样子，数据库完全扛得住，所以优化的肯定是高频接口，QPS比较高的接口，优化收益才大

用Caffeine做本地缓存，那为什么不用Guava，或ConcurrentHashMap，因为咖啡因的读写性能更好，Guava Cache 底层用 ConcurrentHashMap + 分段锁，Caffeine 用的是 ConcurrentHashMap 的增强版 + StripedBuffer，写操作吞吐量高出约 3 倍。，Guava 用 LRU，Caffeine 用 W-TinyLFU，在实际工作负载下的命中率更高。Caffeine 有完整的过期、驱逐、统计能力，ConcurrentHashMap 是纯内存 Map，没有 TTL，需要自己实现过期逻辑，容易出 bug。"

"W-TinyLFU 是 Window Tiny Least Frequently Used，它把缓存分成三个区域：

- Window Cache（约1%）：新数据先放这里，观察是否会再次被访问
- Main Cache（约99%）：热门数据，使用 Segmented LRU 管理
- Probation Cache（约1%）：冷数据的缓冲区，和 Main Cache 的数据做驱逐竞争

相比纯 LRU，W-TinyLFU 解决了两个问题：

1. **扫描污染**：一次性批量扫描数据不会把热门数据挤出去（LRU 会被污染）
2. **频率与新鲜度平衡**：既考虑访问频率也考虑访问时间，不会因为某个数据曾经访问频率高就永远不驱逐

TTL选5秒是我们业务可以接受的数据延迟窗口，这个接口返回的是最近完成的任务通知，用户看到的是一条随机通知，延迟5秒不会影响用户体验，如果TTL太长了，用户看到的可能是一个已经下架的，过期的任务通知了

maximumSize 1000是根据日均接口调用量，QPS大约在500，和数据热度估算的，热点用户的ID在一段时间内是稳定的，1000个基本能覆盖90%的热数据命中

用Caffeine自带的cache stats()可以拿到命中率数据，上线之后通过日志记录了hitCount、missCount、totalLoadTIme，实际命中率大概在80%～92%之间，而且不是稳定的，具体还要取决于业务场景的波动，而排行榜是实时变化的，所以命中率比较低，大约在70%左右

如果是昵称5秒的延迟可以接受，但是如果是余额、订单状态是不能接受的，所以就要通过写穿透+主动失效策略来处理：

1. 更新数据库时，同步删除/更新 Caffeine 缓存中的对应 key
2. 用 `@CacheEvict` 注解在 Service 层标注失效时机
3. 对于分布式多实例场景，用 Redis 发布订阅通知所有实例一起失效本地缓存"

如果数据库更新成功了，但删除缓存失败了，这是一个分布式一致性的问题，使用延迟双删方案解决，

```java
// 1. 先删缓存
cache.invalidate(key);
// 2. 更新数据库
userMapper.update(user);
// 3. 延迟 500ms 后再删一次缓存（兜底）
schedule(() -> cache.invalidate(key), 500, TimeUnit.MILLISECONDS);
```

第一次删缓存，更新数据库，然后延迟再删一次。如果第一次删缓存失败了（比如缓存服务暂时不可用），更新数据库后等 500ms 再删一次，确保新数据生效。之所以不等更久是因为本地 Caffeine 缓存本身 TTL 只有 5 秒，500ms 后再删一次基本能兜住。"

"延迟双删引入了异步逻辑，如果两次删除之间有请求进来，读到的还是旧数据怎么办？"

**标准回答**：

> "这在分布式场景下是不可避免的短暂不一致。但对于我们的业务场景（通知列表），500ms 的短暂不一致完全可接受。对于强一致性场景（如余额），我会用**数据库事务 + 缓存预热**的方案：先更新数据库并提交事务，事务提交成功后再更新缓存，用事务的原子性保证缓存不回退到更旧的状态。"

"Caffeine 的 `get(key, mappingFunction)` 方法自带**原子性保证**——同一个 key 在并发访问时，`mappingFunction` 只会执行一次。底层用的是 `ConcurrentHashMap.computeIfAbsent` + `StripedBuffer`，保证了线程安全。

所以即使 100 个线程同时请求同一个 key 的数据，也只会执行一次数据库查询，其余线程等待结果。这天然解决了缓存击穿问题，不需要额外加锁。"

我用的是 `expireAfterWrite`：

- `expireAfterWrite(5s)`：写入后 5 秒，条目过期，下次访问时重新加载
- `refreshAfterWrite(5s)`：写入后 5 秒，**后台异步刷新**，用户拿到的可能是旧数据但不会等

对于通知接口，`expireAfterWrite` 更合适——过期后阻塞当前请求去加载新数据，保证通知内容是最新的。如果用 `refreshAfterWrite`，用户可能看到上一轮的数据。"

"如果 `mappingFunction` 抛出未检异常（RuntimeException），Caffeine 会**不缓存任何东西**，下次请求同一个 key 会重新执行 mappingFunction。所以我的做法是在 mappingFunction 里做异常捕获：



```java
User user = userCache.get(userId, id -> {
    try {
        return userMapper.findById(id);
    } catch (Exception e) {
        log.error("缓存加载失败, userId: {}", id, e);
        return null;  // 返回 null，不缓存
    }
});
if (user == null) {
    // 降级处理：返回默认值或抛业务异常
}
```

这样既不会让异常扩散到缓存层，也有降级兜底。"

从480ms优化到40ms，是通过线上的真实流量A/B对比的。上线前在测试环境中通过JMeter进行压测处理了，模拟线上真实qps进行压测，QPS=500，记录优化前的p50/p95/p99的耗时

然后进行上线优化，在同一QPS下继续进行压测，再记录一次p50/p95/p99的情况

同时在线上用AOP切面记录每个请求的总耗时和数据库查询次数，写入日志

用Grafana看上线前后的RT分布变化

480ms 是优化前 p95 的耗时（平均可能更低，但 p95 更能反映用户感知），40ms 是优化后 p95 的耗时。优化前后数据库查询次数从 21 条降到 3 条，网络往返减少是 RT 下降的主要原因。"

"p99 大约是 1.2 秒，这是因为随机网络抖动导致 N+1 的 20 次往返累积放大了。优化后 p99 降到约 120ms，改善幅度更大。这也说明 N+1 在网络抖动下影响比平均值反映的更严重。"

我的Caffeine用@Bean注入到Spring容器中，不同Service注入到同一个@Bean中，就会共享同一个缓存实例，所以不同的Service都可以通过依赖注入注入到同一个userCache中，避免了缓存冗余

Caffeine本地缓存天然是单实例的，如果部署了4个实例，那么每一个实例的Caffeine本地缓存都是独立的，如果一个用户修改了昵称，对于这四个不同的，独立的实例，数据不一致的话，要么就等待TTL5秒，然后各个实例自动国旗，多远通知类业务，不一定可以接受，要么就是手动去处理，进行主动同步操作，就是所有的实例收到后主动清理本地缓存，通过Redis Pub/Sub广播失效事件来清理本地缓存

对于一些核心的业务，例如余额、订单等信息就进行主动同步处理，对于一些通知类的，不是那么重要的就容忍

本地缓存和Redis缓存的定位不一样

> | 维度       | Caffeine 本地缓存      | Redis                   |
> | ---------- | ---------------------- | ----------------------- |
> | 延迟       | ~0.1ms（内存直接读）   | ~1-3ms（网络往返）      |
> | 吞吐量     | 极高                   | 受限于 Redis 单节点吞吐 |
> | 数据一致性 | 单实例，需额外同步     | 天然分布式一致          |
> | 适用场景   | 高频读、允许短暂不一致 | 需要全局一致的共享数据  |
>
> 对于通知接口这种**读 QPS 很高、数据变更频率低、允许短暂不一致**的场景，Caffeine 本地缓存是更好的选择——直接从 JVM 堆内存读取，连网络延迟都没有。对于需要全局一致的数据（比如排行榜分数），我用 Redis。"

caffeine maximum 设的是1000，但实际上有10000个活跃用户在短时间内同时请求，那么caffeine 就会使用w-tinylfu驱逐策略，当容量达到1000时，新数据进行会触发驱逐，wtinylfu的核心优势就是即使缓存容量远小于 工作集大小，缓存命中率依然很高

具体就是：

- 10000个用户实际上真实的热点用户可能就200-300个
- wtinylfu会自动识别并保留这200-300个热点数据
- 冷数据进来就会被放在windows cache中，如果不再次访问就会被驱逐

> 所以即使缓存远小于用户量，实际命中率仍然可以维持在 80%+。这也是为什么选 Caffeine 而不是 Guava Cache 的核心原因——Guava 的 LRU 在缓存不够大时命中率会显著下降。"

如果出现了缓存雪崩——比如所有缓存同时过期，同时 500 个请求打到数据库，你怎么做？"

**标准回答**：

> "缓存雪崩在我这个场景下发生的概率较低，因为 Caffeine 是本地缓存，每个 key 的过期时间是独立计算的（expireAfterWrite），不是同时设置的，所以天然分散了。
>
> 但如果真的发生了（比如服务重启后所有缓存失效），我会用 Caffeine 的 `refreshAfterWrite` 替代 `expireAfterWrite`——条目过期后不是阻塞等待加载，而是返回旧值、后台异步刷新，这样请求不会堆积到数据库。
>
> 另外，数据库连接池本身就有保护机制（HikariCP 的 maximumPoolSize），如果请求量超过连接池上限，多余的请求会排队等待，不会直接把数据库打死。"

caffeine是java高性能本地缓存库，是springboot的默认缓存实现

- maximum(1000)最多缓存1000个条目，超出按驱逐策略
- expireAfterwrite(5s)写入后5s过期，下次读取触发重新加载机制
- refreshAfterWrite(5s)：写入后5s后台刷新，读取时返回旧值不阻塞
- recordStates()开启统计，命中率统计、加载耗时统计

驱逐策略W-TinyLFU：

windows(1%)->Main(99%)->Probation(1%)

新数据先进入的是windows，访问频率高的就晋升到Main中，访问频率比较低的就会降级到probation，和main里的旧数据竞争淘汰，Count-Min Sketch做频率统计，4-bit计数器，省内存，比LRU的优势是抗扫描污染，缓存不够大时命中率依然很高
线程安全

```
cache.get(key, k -> loadFromDB())
→ 同一 key 并发时，loadFromDB() 只执行一次
→ 其他线程等待结果返回
→ 天然防缓存击穿，不需要额外加锁
```

----

# 针对第二条实习产出

简历上写的是设计复合索引/联合索引，对SELECT*这个查询语句进行优化

我们以task表作为一个例子，这个表格有35个字段，其中包括多个TEXT大字段，而且在优化前进行的是select*查询，每次select*查询都会查询这几个大字段，例如任务说明、二维码说明、二维码链接、二维码地址、营销折扣JSON，基本上都会占用几kb的内存，在优化前使用select*查询，每一行大约是2～5kb的内存。那么一共20行的话，大约就是40～100kb的内存，进行网络传输

那么进行优化之后，就不用select*进行查询了，通过创建联合索引来进行查询操作，每一行传递9个字段，而且传递的这9个字段都是业务需要的，就不会包含TEXT大字段了，所以对于这20行就可以优化至4～10kb的网络传输，传输量减少了将近80%

那么复合/联合索引是怎么设计的呢，就是对task_user_id、task_name、task_per_price、task_status、create_time进行创建联合索引

那么在优化之前没有索引的查询过程就是查询出所有的字段，all types，执行过程就是查询出50000行数据，进行全表扫描，然后进行逐行判断，去判断task_staus=2 using where，然后通过文件排序，filesort using filesort对这50000行数据进行排序，效率极慢，然后排序完之后只会取前20行的数据

那么通过创建复合/联合索引之后，那么通过explain解析后得到的结果就是进行range查询，就是进行范围查询，大约只会查询150行数据这样子，在查询过程中，会走索引idx_status_user_time，根据最左前缀法则，task_status=2定位到索引树的某个节点，然后在索引内部继续进行过滤，通过task_user_id进行过滤，继续通过索引内部进行过滤，就是create_time进行过滤，进行范围+排序，天然是有序的，就不需要使用filesort，那么不实用fielsort就可以提高效率，然后回表查出20行的完整数据即可

但是即使进行了上面的优化之后，还是需要进行回表查询，效率并没有优化到极致，所以还可以通过覆盖索引来避免回表查询的使用，就是通过explain分析得到结果，using index使用覆盖索引，不会进行回表查询，他会直接从索引树取出数据，不访问数据行，耗时减少了30～50%

-----

# 针对第三条实习产出

将5独立查询合并成1条JOIN查询和1条COUNT查询，减少了网络往返RTT

在优化前是执行5条独立校验，执行5条独立的SQL操作，这五条独立的SQL分别是同名检查task_order+task，举报检查task_complaint，未绑定微信限制task_order，占用数检查task_order，等级检查level，这五条是原始的5条独立的SQL查询，那么对于这5条原始的独立的SQL，就需要进行5次的网络往返RTT，每一条SQL的网络往返大约就是3ms，那么五条就是15ms，然后加上业务处理耗时，一共就是350ms，一共包含的是五次RTT耗时各3ms+5次DB执行+业务逻辑

那么进行优化，优化就是将这5条SQL合并成一条JONSQL和一条COUNTSQL来进行操作，分别是任务测校验和用户侧校验，任务侧校验就是：同名任务检查，占用数检查、未绑定微信限制，用户侧校验就是举报检查，而且等级检查数据量绩效，可能只有5～10行数据量，直接用caffeine缓存即可，所以只有两次RTT网络往返各3ms，然后两次DB执行，还有就是缓存读取，一共就30ms

为什么合并成两条SQL而不是合并成一条SQL呢

```
task_order + task 表 → 数据关联强（通过 task_id 关联），可以 JOIN
task_complaint 表 → 数据独立（用户举报记录），JOIN 会产生不必要的笛卡尔积膨胀
level 表 → 配置数据，量小变化少，用缓存即可
```

所以是按照数据关联性和变化频率来进行分组的，两条SQL一个缓存，就是最方案

优化之后的RTT就8ms

----

# 针对第四条实习产出

针对这个NoRepeatSubmit进行组件重构

原始的key设计是通过String redisKey来进行设计的，使用了SETNX设置锁，过期时间设置成了3s，3s过期

那么原始的key设计导致了什么问题呢，我举一个业务场景的例子，用户想要提现两笔金额不同的钱，一笔10，一笔20，然后就发送第一次请求，去提现第一笔金额10元，这个时候就会有一个key，然后通过setnx成功设置锁，设置过期时间，写入Redis，然后进行具体业务执行，业务执行成功后，然后就在setnx设置的过期时间之后，发送第二次请求，你会发现这个请求的key的toekn和第一次key的token是一样的，甚至可以说是完全相同的key，那么setnx就会失败，设置锁失败，就会被拦截，那么用户就会收到一个提示信息，操作频繁，但是这显然是两笔不同金额的提现，不应该拦截，所以就会导致一个问题，就是同一个用户同一个接口不同业务操作被误判为重复提交

那么就要进行优化，怎么进行优化呢，有三个方案，方案1 就通过加入流水号来进行优化，每一笔金额的提现都进行一个流水号的分配，每次请求都会由前端传过来，前端生成的UUID，requestId接受前端的UUID，每次请求前端生成的这个UUID都是不一样的，方案2，如果是相同的请求，并且相同的MD5就进行拦截，请求相同但是MD5不一样就允许放行，方案3就是通过bizId，bizId作为唯一的业务表示id，例如订单号或任务ID

优化前用户发送请求，他的key就是类名_方法名_token，优化前相同的用户相同的接口不同的业务就会生成一个相同的key，就会导致误判，拦截了不该被拦截的请求，给出提示信息，操作频繁

那么进行优化后，用户发送请求，就会在原来的key的基础上多加一个业务流水号，前端生成UUID作为业务流水号，相同的请求会被拦截，不同的请求不会被拦截

而且还进行了合并两个切面的优化，因为会发现在原始代码中有两个功能几乎一样的切面，就是NoRepeatSubmitAspect和NoRepeatSbumitAspect2这两个类，优化后就会合并成一个，通过Spel动态拼接key，通过注解成熟动态配置

# 针对第五条实习产出

拦截疑似黑名单的用户，防止别人黑产账号，设计一套规则引擎和统一的架构

在进行设计之前，即进行优化前，原始代码是散落的规则，并且没有统一架构，在原始的withdrawAliPay()这个方法中有12+条校验规则，全部散落在一个方法里，顺序逻辑靠if-else来堆叠的

```
原始 withdrawAliPay() 方法中的校验（散落式）：

1. 黑名单检查          → if 黑名单存在且未过期 → 抛异常
2. Redis限流           → SETNX 防同支付宝号频繁提现
3. 首次/非首次最小金额  → if 金额 < config最小 → 抛异常
4. 金额必须为整数       → 正则匹配 → 抛异常
5. 金额过大需审核       → if 金额 > config阈值 → 抛异常
6. 收益比检查           → if 收益比 < 0.3 → 需人工审核
7. 支付宝账号绑定检查   → if 账号已被其他人绑定 → 抛异常
8. 同名每日上限         → if 同名提现 >= 10 → 抛异常
9. 有效用户检查         → if 没完成过任务 → 需审核
10. 推广者检查          → if 是推广者但无活跃徒弟 → 抛异常
11. 当日提现次数上限    → if 次数 >= config → 抛异常
12. 余额充足检查        → if 余额不足 → 抛异常
```

问题：

- 规则散落，没有统一的管理
- 新值的规则需要修改原方法，不符合开闭原则
- 没有IP风控检测、接单异常检测、账号关联分析
- 没有异步审计和自动加入黑名单机制
- 审核全靠人工，apply状态等待人工操作

那么就得进行优化。通过设计规则引擎+异步审计流来进行优化

首先提现申请入口，用户发送提现请求，这个请求携带userId、amout、apipayAccounapipayname

然后渠道责任链，主要有多个规则，黑名单检查、金额检查、支付宝关联、IP异常检测、接单耗时遗产检测，每一个规则中直接返回拒绝，不再执行后续规则，全部通过，进入下一步

那么这个责任链是怎么设计的呢，责任链设计模式：

- 每个规则实现同一个接口WithdrawRule
- 规则之间解耦，新增规则只需实现接口+加入链
- 复合开闭原则，不需要修改原有的代码

如果全部都通过，就提现记录写入status=APPLY

然后发送异步通知，在异步层进行定时任务

每一个小时执行一次的行为分析任务，首先是接单频率的分析，异常是30s完成任务或每天接50+单

然后是设备指纹聚类，异常是同一设备操作3+账号

接着是邀请链深度分析，异常是疑似自邀自刷

最后是提现频次异常，异常是七天提现5+而且总金额异常

复合规则的账号就会被自动写入到黑名单表中

然后黑名单自动生效，下次该用户提现时：

- 在实时层Rule1黑名单检测命中，直接拒绝
- 形成闭环，进行异步检测，然后自动加入黑名单，最后进行实时拦截

责任链的代码结构，首先就是规则接口，然后规则实现示例：IP异常检测、接单耗时异常

然后就是责任链执行器

整体的一个数据流就是：

```
用户提现
  │
  ▼
实时责任链（毫秒级）
  ├── 黑名单检查 → 拒绝
  ├── 金额校验 → 拒绝
  ├── 支付宝关联 → 拒绝
  ├── IP异常 → 拦截
  └── 接单耗时异常 → 拦截
  │
  ├── 全部通过 → 写入提现记录（APPLY状态）
  ▼
异步定时任务（小时级）
  ├── 接单频率分析 → 符合规则 → 加黑名单
  ├── 设备指纹聚类 → 同设备多号 → 加黑名单
  ├── 邀请链分析 → 自邀自刷 → 加黑名单
  └── 提现频次异常 → 加黑名单
  │
  ▼
黑名单生效
  └── 下次提现时实时层拦截
```
